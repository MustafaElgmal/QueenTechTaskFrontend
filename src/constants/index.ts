export const BASE_URL = "http://localhost:4000";
export const clickArr = [false, false, false, false];
export function classNames(...classes: string[]) {
  return classes.filter(Boolean).join(" ");
}
export const limit=10